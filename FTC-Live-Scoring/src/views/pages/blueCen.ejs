<!DOCTYPE html>
<html lang="en">

<head>
  <% include ../partials/head-blue-single %>
</head>

<body>
  <header>
  </header>
  <main onclick="requestFullScreen(document.body)">

    <div class="wrapper">
      <header class="header">FTC Live Scoring Beta - Both Teams</header>
      <article class="main">
        <button id="cenbun" type="button" class="btn btn-primary btn-lg btn-block">
          <span class="bcen">0</span>
        </button>
      </article>

      <aside class="aside aside-2">
        <button id="b1m" type="button" class="btn btn-primary btn-lg btn-block">-</button>
      </aside>

      <aside class="aside aside-4">
        <button id="b1p" type="button" class="btn btn-primary btn-lg btn-block">+ Center +</button>
      </aside>
      <footer class="footer">Sokolov©</footer>
    </div>

  </main>



  <!--<script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>-->
  <script src="/assets/js/jquery-1.10.2.min.js"></script>
  <script src="/socket.io/socket.io.js"></script>
  <script src="/assets/js/blueCen.js">
  </script>

  <script type="text/javascript">
    //document.getElementById("b1p").onclick = function() {add('.bcen', 3)};
    //document.getElementById("b1m").onclick = function() {sub('.bcen', 3)};

    //document.getElementById("b2p").onclick = function() {add('.bcor', 4)};
    //document.getElementById("b2m").onclick = function() {sub('.bcor', 4)};
  </script>

     <script type="text/javascript">
     function requestFullScreen(element) {
    // Supports most browsers and their versions.
    var requestMethod = element.requestFullScreen || element.webkitRequestFullScreen || element.mozRequestFullScreen || element.msRequestFullScreen;

    if (requestMethod) { // Native full screen.
      requestMethod.call(element);
    } else if (typeof window.ActiveXObject !== "undefined") { // Older IE.
      var wscript = new ActiveXObject("WScript.Shell");
      if (wscript !== null) {
        wscript.SendKeys("{F11}");
      }
    }
  }

var elem = document.body; // Make the body go full screen.
requestFullScreen(elem);
</script>


<script>
  window.navigator = window.navigator || {};
  if (navigator.vibrate === undefined) {
    document.getElementById('v-unsupported').classList.remove('hidden');
    ['b1p', 'b1m'].forEach(function(elementId) {
      document.getElementById(elementId).setAttribute('disabled', 'disabled');
    });
  } else {
    ['b1p'].forEach(function(elementId) {
      document.getElementById(elementId).addEventListener('click', function() {
        navigator.vibrate(250);
      });
    });
    ['b1m'].forEach(function(elementId) {
      document.getElementById(elementId).addEventListener('click', function() {
        navigator.vibrate([50, 100, 50]);
      });
    });   
    ['cenbun'].forEach(function(elementId) {
      document.getElementById(elementId).addEventListener('click', function() {
        navigator.vibrate(0);
      });
    }); 
  }
</script>   

<% include ../partials/scripts %>
</body>

</html>